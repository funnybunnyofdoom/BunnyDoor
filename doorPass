--This program will listen for the disk and
--display the status
--it will also give access to parts of the lab

while true do
    
  local event,side = os.pullEvent()
  if event == "monitor_touch" then
    doorgui.timeSelector()
  end
end
